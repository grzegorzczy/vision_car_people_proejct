# ============================================================
#  Vision + Finder Opta — zaleznosci Pythona
#  Python 3.12 / Windows / GPU NVIDIA (CUDA)
# ============================================================
#
# UWAGA dot. PyTorch + CUDA:
#   Wersji z CUDA NIE instalujemy zwyklym "pip install torch".
#   Najpierw instalujemy torch z oficjalnego indeksu CUDA, np. (CUDA 12.1):
#
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   Dopiero potem: pip install -r requirements.txt
#   (ultralytics NIE nadpisze juz zainstalowanego torcha z CUDA).
# ------------------------------------------------------------

ultralytics>=8.2.0        # YOLOv8 (Ultralytics)
opencv-python>=4.9.0      # akwizycja obrazu + rysowanie bounding boxow
numpy>=1.26,<2.0          # operacje na macierzach (klatki)
Flask>=3.0.0              # lekki backend HTTP + dashboard (127.0.0.1:5000)
pymodbus>=3.6.0           # klient Modbus TCP (Python -> PLC Opta)
